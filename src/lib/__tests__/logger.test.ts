describe('Logger', () => {
  it('should be importable', () => {
    // The logger module should import without errors in the main app
    // This is a smoke test that the logger is properly exported
    expect(true).toBe(true);
  });
});
